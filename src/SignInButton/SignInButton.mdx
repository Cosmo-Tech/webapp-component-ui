---
name: SignInButton
route: /SignInButton
menu: Buttons
---
import { Playground, Props } from 'docz'
import { SignInButton } from './SignInButton'

# Microsoft Sign In Button

The aim of this component is to easily display a sign in button with Microsoft logo.

We use react-i18next for translation.

If you want to change button's label, you have to define, in your translation files,
json path `genericcomponent.button.login.msal.title`


## Props

<Props of={ SignInButton }/>

## Basic Usage

<Playground>
    <SignInButton
      onClick={() => alert('Use auth provider you want ')}
    />
</Playground>
