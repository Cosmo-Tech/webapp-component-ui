---
name: CreateScenarioButton
route: /CreateScenarioButton
menu: Buttons
---

import { Playground, Props } from 'docz'
import { CreateScenarioButton } from './CreateScenarioButton'

# Create Scenario Button

The aim of this component is to easily display a create scenario button.
This button will display a popup that guide users through scenario creation

### Props

<Props of={ CreateScenarioButton }/>

### Basic Usage

<Playground>
      <CreateScenarioButton
      labels={
        {
            button : {
                title: 'Create a Scenario',
                tooltip: 'This is a tooltip'
            },
            dialog: {
              title: 'Create a Scenario',
              scenarioName: 'Scenario Name',
              scenarioMaster: 'Master',
              scenarioParent: 'Parent scenario',
              datasetPlaceholder: 'Dataset',
              dataset: 'Select a dataset',
              scenarioTypePlaceholder: 'Scenario',
              scenarioType: 'Scenario Type',
              cancel: 'Cancel',
              create: 'Create'
            },
            errors: {
             emptyScenarioName:'Scenario name should not be empty',
             existingScenarioName:'Scenario name already exists',
             forbiddenCharsInScenarioName:'Scenario name is not valid'
            }
        }
      }
        solution={
        {
            name: 'Solution',
            id: 'SOL-000001'
          }
        }
        workspaceId={'workspaceId'}
        createScenario={() => alert('Scenario creation')}
        currentScenario={{data:null}}
        runTemplates={
            [
                {
                  id: '1',
                  name: 'A wonderful run template name',
                }
            ]
        }
        datasets={[
         {
              id: 'D-000001',
              name: 'A wonderful dataset name',
              tags: [
                'dataset'
              ]
        }
        ]}
        scenarios={[]}
        user={
            {
              userId: 'This is a UUID',
              userName: 'Bob Bobinson',
              status: 'AUTHENTICATED'
            }
        }
        disabled={false}
        buttonTooltip={'This is a tooltip'}
        nameValidator={/^\w[\w\d\s-]*$/}
      />
</Playground>
